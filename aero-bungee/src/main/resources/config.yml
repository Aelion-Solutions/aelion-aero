# Aelion Aero — operator / template-safe settings (no secrets).
# Identity (panel URL, tokens, control, insecure SSL) lives in aero.ae
# (injected by Aelion Cloud). Manual installs: create aero.ae next to this file.
config-version: 1
