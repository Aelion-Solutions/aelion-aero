# Aelion Aero — injected by Aelion Cloud at provision time.
panel-url: ""
server-id: ""
token: ""
# DEV ONLY: skip TLS cert/hostname checks for panel HTTPS (self-signed / mkcert).
panel-insecure-ssl: false

# Daemon localhost bridge for live proxy backend sync.
control:
  enabled: false
  bind: "127.0.0.1"
  port: 25580
  token: ""
